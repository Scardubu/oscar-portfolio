// Portfolio metrics API route placeholder (Phase 4)
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'Metrics API placeholder' }, { status: 501 });
}
