// SabiScore preview API route placeholder (Phase 2)
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'SabiScore API placeholder' }, { status: 501 });
}
